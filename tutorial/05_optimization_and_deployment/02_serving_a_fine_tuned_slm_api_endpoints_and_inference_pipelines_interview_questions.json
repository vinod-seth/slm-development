{
  "lesson_title": "API Endpoints and Inference Pipelines",
  "experience_levels": {
    "junior": [
      {
        "id": "m5_l2_j1",
        "question": "What is continuous batching, and how does it improve serving throughput?",
        "complexity": "Easy",
        "companies": [
          "General",
          "vLLM"
        ],
        "core_intent": "Assesses basic knowledge of serving optimization.",
        "preparation_guide": "Contrast static request batching with iteration-level request scheduling.",
        "explanation": "Static batching groups requests together, keeping GPUs idle until the longest response completes. Continuous batching schedules requests at the iteration level. Finished requests are ejected and new ones are inserted immediately, minimizing GPU idle time.",
        "star_framework_tip": "Use a queuing analogy to explain the differences in scheduling.",
        "common_pitfalls": [
          "Claiming continuous batching reduces single-query latency (it increases throughput, not single-query latency)."
        ],
        "references": [
          {
            "title": "vLLM Paper",
            "url": "https://arxiv.org/abs/2309.06180"
          }
        ]
      }
    ],
    "mid_level": [
      {
        "id": "m5_l2_m1",
        "question": "Build a FastAPI server in Python that loads a Hugging Face model and exposes a streaming response endpoint.",
        "complexity": "Medium",
        "companies": [
          "FastAPI",
          "General"
        ],
        "core_intent": "Tests hands-on web serving and asynchronous coding.",
        "preparation_guide": "Use FastAPI's StreamingResponse and TextIteratorStreamer from transformers.",
        "explanation": "```python\nfrom fastapi import FastAPI\nfrom fastapi.responses import StreamingResponse\nfrom transformers import AutoModelForCausalLM, AutoTokenizer, TextIteratorStreamer\nfrom threading import Thread\n\napp = FastAPI()\ntokenizer = AutoTokenizer.from_pretrained(\"HuggingFaceTB/SmolLM2-135M-Instruct\")\nmodel = AutoModelForCausalLM.from_pretrained(\"HuggingFaceTB/SmolLM2-135M-Instruct\")\n\n@app.get(\"/generate\")\ndef generate(prompt: str):\n    streamer = TextIteratorStreamer(tokenizer, skip_prompt=True)\n    inputs = tokenizer(prompt, return_tensors=\"pt\")\n    \n    thread = Thread(target=model.generate, kwargs={\n        **inputs, \"streamer\": streamer, \"max_new_tokens\": 50\n    })\n    thread.start()\n    \n    def generator():\n        for text in streamer:\n            yield text\n            \n    return StreamingResponse(generator(), media_type=\"text/plain\")\n```",
        "star_framework_tip": "Run model generation in a background thread to prevent blocking FastAPI's async loop.",
        "common_pitfalls": [
          "Blocking the event loop by running generation synchronously inside the endpoint definition."
        ],
        "references": []
      }
    ],
    "senior": [
      {
        "id": "m5_l2_s1",
        "question": "Explain the concept of Speculative Decoding. How does it leverage a tiny draft model to speed up a target model's generation?",
        "complexity": "Hard",
        "companies": [
          "Google",
          "DeepMind",
          "Meta"
        ],
        "core_intent": "Assesses knowledge of state-of-the-art serving architectures.",
        "preparation_guide": "Describe draft generation passes, verification passes, and acceptance rates.",
        "explanation": "Speculative decoding uses a small, fast model (the draft model) to generate $K$ tokens. The target model then runs a single forward pass to verify all $K$ tokens in parallel. If tokens match the target model's output distribution, they are accepted. This reduces target model execution steps, improving latency.",
        "star_framework_tip": "Provide the speedup math based on average draft model acceptance rates.",
        "common_pitfalls": [
          "Believing speculative decoding requires retraining the target model (it is a training-free serving optimization)."
        ],
        "references": [
          {
            "title": "Fast Inference from Transformers via Speculative Decoding",
            "url": "https://arxiv.org/abs/2211.17111"
          }
        ]
      }
    ]
  }
}