{
  "lesson_title": "Memory Footprint and Hardware Tiers for SLM Training",
  "experience_levels": {
    "junior": [
      {
        "id": "m2_l2_j1",
        "question": "How much GPU memory (VRAM) is strictly required to store the weights of a 1.5B parameter model in FP32 vs. FP16 precision?",
        "complexity": "Easy",
        "companies": [
          "NVIDIA",
          "Meta"
        ],
        "core_intent": "Tests basic weight memory calculations.",
        "preparation_guide": "Recall byte mappings: FP32 = 4 bytes, FP16 = 2 bytes, INT8 = 1 byte.",
        "explanation": "Model weight size is calculated as $Parameters \\times BytesPerParam$. For a 1.5B model: FP32 requires $1.5\\text{B} \\times 4\\text{ bytes} = 6\\text{ GB}$ of VRAM. FP16/BF16 requires $1.5\\text{B} \\times 2\\text{ bytes} = 3\\text{ GB}$ of VRAM.",
        "star_framework_tip": "Keep calculations clear and state parameters to bytes conversions.",
        "common_pitfalls": [
          "Confusing optimizer VRAM requirements with pure weight storage requirements."
        ],
        "references": []
      }
    ],
    "mid_level": [
      {
        "id": "m2_l2_m1",
        "question": "Why does training a model with AdamW under mixed precision (FP16/FP32) require much more VRAM than weights storage, and how is it distributed?",
        "complexity": "Medium",
        "companies": [
          "Google",
          "NVIDIA",
          "Hugging Face"
        ],
        "core_intent": "Tests memory mapping for weights, gradients, and optimizer states.",
        "preparation_guide": "Detail the parameters storage under mixed precision training configurations.",
        "explanation": "Mixed precision training stores: 1) FP16 weights (2 bytes/param), 2) FP16 gradients (2 bytes/param), and 3) AdamW optimizer states in FP32 (8 bytes/param: 4 bytes master weight + 4 bytes first momentum + 4 bytes second momentum = 12 bytes total per parameter for optimizer states). Combined, this is 16 bytes per parameter, excluding activations and model inputs.",
        "star_framework_tip": "Deconstruct AdamW optimizer state allocations step-by-step to show calculation depth.",
        "common_pitfalls": [
          "Omitting the FP32 master weights replica in the optimizer calculation."
        ],
        "references": [
          {
            "title": "Mixed Precision Training (Micikevicius et al.)",
            "url": "https://arxiv.org/abs/1710.03740"
          }
        ]
      }
    ],
    "senior": [
      {
        "id": "m2_l2_s1",
        "question": "Write a Python script to estimate the peak VRAM footprint for training a model with $P$ parameters under a batch size $B$ and sequence length $S$.",
        "complexity": "Hard",
        "companies": [
          "AWS",
          "Google",
          "Meta"
        ],
        "core_intent": "Tests analytical coding of memory budgets.",
        "preparation_guide": "Write a profiling script implementing equations for model weights, gradients, optimizer states, and activation estimates.",
        "explanation": "```python\ndef calculate_vram_training(params_billions, batch_size, seq_len, hidden_dim, layers, precision='fp16_amp'):\n    # Weights & Gradients & Optimizer States (AdamW mixed-precision)\n    # 2 bytes weight (FP16) + 2 bytes grad (FP16) + 12 bytes optimizer (FP32 master weight + momentums) = 16 bytes/param\n    fixed_bytes = params_billions * 1e9 * 16\n    \n    # Activation memory estimate (quadratic in self-attention: B * S^2 * H * L)\n    # Highly simplified model: actual activation memory depends on layer normalization and intermediate widths\n    activation_bytes = batch_size * (seq_len ** 2) * hidden_dim * layers * 2\n    \n    total_gb = (fixed_bytes + activation_bytes) / (1024 ** 3)\n    return {\n        \"fixed_gb\": fixed_bytes / (1024 ** 3),\n        \"activations_gb\": activation_bytes / (1024 ** 3),\n        \"total_gb\": total_gb\n    }\n\n# Profile a 1.5B parameter model\nprint(calculate_vram_training(1.5, 4, 2048, 2048, 24))\n```",
        "star_framework_tip": "Note how activation memory scale factors differ depending on whether activation checkpointing is active.",
        "common_pitfalls": [
          "Ignoring activation memory dependency on layer count and sequence lengths."
        ],
        "references": [
          {
            "title": "Reducing Activation Memory in Transformers",
            "url": "https://arxiv.org/abs/2205.14135"
          }
        ]
      }
    ]
  }
}