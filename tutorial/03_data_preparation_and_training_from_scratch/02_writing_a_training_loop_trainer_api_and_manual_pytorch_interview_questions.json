{
  "lesson_title": "Trainer API and Manual PyTorch",
  "experience_levels": {
    "junior": [
      {
        "id": "m3_l2_j1",
        "question": "What is the purpose of gradient accumulation during model training?",
        "complexity": "Easy",
        "companies": [
          "General",
          "Google"
        ],
        "core_intent": "Verifies basic optimization configurations.",
        "preparation_guide": "Discuss virtual batch size optimization under memory constraints.",
        "explanation": "Gradient accumulation aggregates gradients over multiple forward passes before calling `optimizer.step()`. This simulates training with a larger virtual batch size without exceeding GPU VRAM limitations.",
        "star_framework_tip": "Clearly show the relationship between physical batch size, accumulation steps, and effective batch size.",
        "common_pitfalls": [
          "Forgetting to divide gradients by the accumulation steps count, which inflates step updates."
        ],
        "references": []
      }
    ],
    "mid_level": [
      {
        "id": "m3_l2_m1",
        "question": "Write a manual PyTorch training loop incorporating mixed precision (`torch.cuda.amp.GradScaler`) and gradient norm clipping.",
        "complexity": "Medium",
        "companies": [
          "Meta",
          "NVIDIA",
          "Google"
        ],
        "core_intent": "Tests advanced manual PyTorch model training implementations.",
        "preparation_guide": "Incorporate autocast contexts, scalers, backward scales, unscale bounds, and step updates.",
        "explanation": "```python\nimport torch\n\ndef train_step(model, optimizer, dataloader, device, max_norm=1.0):\n    model.train()\n    scaler = torch.cuda.amp.GradScaler()\n    \n    for batch in dataloader:\n        optimizer.zero_grad()\n        inputs = batch['input_ids'].to(device)\n        labels = batch['labels'].to(device)\n        \n        with torch.cuda.amp.autocast():\n            outputs = model(inputs, labels=labels)\n            loss = outputs.loss\n            \n        scaler.scale(loss).backward()\n        scaler.unscale_(optimizer)\n        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm)\n        scaler.step(optimizer)\n        scaler.update()\n```",
        "star_framework_tip": "Note how `scaler.unscale_(optimizer)` must be executed before applying gradient norm clipping.",
        "common_pitfalls": [
          "Applying gradient clipping directly without unscaling, which leads to incorrect gradient norm values due to loss scaling."
        ],
        "references": [
          {
            "title": "PyTorch GradScaler Guide",
            "url": "https://pytorch.org/docs/stable/amp.html"
          }
        ]
      }
    ],
    "senior": [
      {
        "id": "m3_l2_s1",
        "question": "How do you detect, diagnose, and resolve NaN loss values during training runs of an SLM in mixed-precision?",
        "complexity": "Hard",
        "companies": [
          "OpenAI",
          "Meta",
          "Google"
        ],
        "core_intent": "Tests deep troubleshooting in gradient stability and numerical arithmetic.",
        "preparation_guide": "Describe loss scaling underflow, exploding gradients, optimizer settings, and gradient hooks.",
        "explanation": "NaN losses are caused by underflow/overflow under FP16. Solve by: 1) enabling PyTorch anomaly detection (`torch.autograd.set_detect_anomaly(True)`), 2) implementing gradient clipping to cap extreme weights updates, 3) switching to BF16 (wider dynamic range than FP16), or 4) scaling down learning rates and adding warmup steps.",
        "star_framework_tip": "Contrast the numerical range differences of FP16 vs BF16 to show hardware precision depth.",
        "common_pitfalls": [
          "Only reducing batch size to solve NaN loss errors, which does not address the underlying numerical precision issues."
        ],
        "references": []
      }
    ]
  }
}